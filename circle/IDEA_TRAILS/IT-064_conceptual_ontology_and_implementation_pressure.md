# IT-064 — Conceptual Ontology and Implementation Pressure

**Status:** OPEN
**Registry:** #64 · **Opened:** 2026-07-17 · **Observers:** Nova (field-desk), P.H. Claude (filing)
*(Field-desk id at ordering: "IT-065" — recurring off-by-one; registry id is canonical.)*

- **Core trajectory:** model → operational definition → prototype →
  implementation constraint → conceptual revision.
- **Seed:** SWEEP_10's Concept–Operationalization Audit — both principles
  permanent: *not immediately implementable ≠ conceptually worthless* AND
  *conceptually appealing ≠ operationally meaningful*.
- **The return condition (binding):** deferred implementation objections
  need a defined re-admission point — "structure first" must never become
  permanent insulation from practical testing; the sequence is iterative
  (ontology → operationalization → pressure → revised ontology), never
  one-way.
- **What a mature model eventually owes:** unit of analysis, state
  variables, transition conditions, observables, error cases, success
  functions, disconfirmation conditions.
- **Branches:** IT-072 (models that permit their own defeat); IT-032
  (formal-authority discipline — what happens when vocabulary outruns
  operationalization); the SW07 research frameworks (decoder installation
  — the same validation debt).
- **Unfinished:** the Objection-Timing Audit's return condition
  demonstrated in use; whether the SW10 model survives its own
  operationalization demand.
- **Sources:** `../evidence/chat_extractions/DIG_COMPOUND_SWEEP_10_stochastic_diminishing_roles_noise_and_persuasion_frames.md`
  (candidate-grade; no promotions).
