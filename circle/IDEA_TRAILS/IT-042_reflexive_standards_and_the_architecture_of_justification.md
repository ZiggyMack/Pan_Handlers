# IT-042 — Reflexive Standards and the Architecture of Justification

**Status:** OPEN
**Registry:** #42 · **Opened:** 2026-07-17 · **Observers:** Nova (field-desk), P.H. Claude (filing)
*(Field-desk id at ordering: "IT-043" — recurring off-by-one; registry id is canonical.)*

- **Core trajectory:** criterion → self-application → circularity →
  presupposition → reciprocal support → provisional authority.
- **Seed:** DIG_AUX_07's Reflexive Criterion Testing — "how do we know a
  prediction succeeded?" — prediction is not self-interpreting (needs
  identification, comparison, and evaluation conditions).
- **The narrow conclusion (binding):** *prediction requires logical,
  semantic, and evaluative conditions to function as an epistemic
  criterion* — NOT *prediction is therefore secondary in every sense*, and
  NOT *every reciprocal or circular relationship is vicious* (coherentist
  and pragmatic systems accept mutual support openly).
- **The permanent addendum (the crown-transfer guard):** the same
  questions are owed to every candidate — *how is logic validated? how are
  axioms distinguished from stipulations? why is foundationalism
  authoritative?* The regress was **relocated, not eliminated** (confirmed
  A3 against the favored architecture).
- **Circularity taxonomy (this trail's job):** vicious circle vs
  reciprocal support vs constitutive grounding vs practical bedrock —
  classifying which is which, per candidate.
- **Branches:** IT-030 (super-frame stopping — the same debt at frame
  scale); IT-021 (reflexive auditing — the circle's method applied to
  itself); IT-043 (rival bedrocks — the comparative face).
- **Unfinished:** a demonstrated-in-use stopping rule; whether the
  reflexive test itself survives reflexive application; the coherentist/
  pragmatist alternatives never run in-thread.
- **Sources:** `../evidence/chat_extractions/DIG_AUX_07_axioms_prediction_logic_and_epistemic_bedrock.md`
  (candidate-grade; no promotions).
