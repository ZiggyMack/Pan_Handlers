# IT-032 — Entropy, Semantic Compression, and Cultural Meaning

**Status:** OPEN
**Registry:** #32 · **Opened:** 2026-07-17 · **Observers:** Nova (field-desk), P.H. Claude (filing)
*(Field-desk id at ordering: "IT-033" — recurring off-by-one; registry id is canonical.)*

- **Core trajectory:** Shannon uncertainty → semantic density → mythic
  compression → decoder architecture → projection and reconstruction
  constraints.
- **Seed:** COMPOUND_SWEEP_07's information branch — the Shannon–Semantic
  Layer Audit in its corrected five-dimension form (entropy / semantic depth
  / pragmatic value / internalization / cultural persistence).
- **Unlocking distinction (both directions guarded):** *low Shannon surprise
  ≠ low semantic depth* AND *semantic richness ≠ demonstrated compression* —
  the double rule: **unmeasured ≠ zero, and unmeasured ≠ secretly enormous.**
- **Quarantined vocabulary:** "high semantic entropy," "wisdom per bit,"
  "high-dimensional decompression space" — proposed descriptive dimensions,
  never information-theoretic measurements, unless explicitly marked
  metaphor.
- **Five compression senses kept apart:** Shannon / semantic / mnemonic /
  pragmatic / cultural — related metaphors with different predictions, not
  interchangeable definitions (AUX_06's specification debts carry over:
  source, encoding, decoder, success criterion, loss budget).
- **Branches:** IT-026 (mythic compression — the claim-strength ladder);
  IT-035 (decoder installation); IT-038 (formal authority leakage — what
  happens when this trail's vocabulary escapes its jurisdiction).
- **Unfinished:** any operational measure of "semantic compression"; the
  loss budget (which distinctions may be discarded without corrupting the
  alleged wisdom — classified A1: conceptually essential, never
  demonstrated).
- **Sources:** `../evidence/chat_extractions/DIG_COMPOUND_SWEEP_07_entropy_semantic_compression_materialism_and_narrative_transmission.md`
  (candidate-grade; no promotions).
